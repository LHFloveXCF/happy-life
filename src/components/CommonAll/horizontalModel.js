
import classNames from 'classnames';
import style from './index.module.scss';

/**
 * 横线
 * 
 */
const HorizontalModel = () => {
    return (
        <>
            <div className={classNames(style.horizontal_line)}></div>
        </>
    );
};

export default HorizontalModel;